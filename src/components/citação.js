import React from 'react';
import '../styles/testimonialSection.css';

const TestimonialSection = () => {
  return (
    <section className="testimonial-section">
      <div className="testimonial-content">
        <blockquote>
          “A inteligência artificial não está apenas mudando o mundo, ela está redefinindo a forma como entendemos e interagimos com a realidade.”
        </blockquote>
        <p className="client-name">— Usuário da IA, Transformando a forma de pensar</p>
      </div>
    </section>
  );
};

export default TestimonialSection;
